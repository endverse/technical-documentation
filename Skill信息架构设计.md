# 文档规范 Skill 信息架构设计

> 阶段：B — 信息架构与路由设计。
>
> 目的：在创建正式 Skill 文件前，明确入口、共同规范、类型规范和参考资料的职责、读取顺序及引用关系。

## 1. 设计目标

正式 Skill 必须让 Agent 在文档任务中完成以下判断：

1. 用户要解决什么问题，应该采用哪一种文档类型。
2. 用户未指定类型时，需要问什么最少问题，并在何时等待确认。
3. 需要读取哪些规则，避免一次加载全部 11 类模板。
4. 如何把共同的 130 条约束和类型专属结构结合，而不重复或冲突。
5. 何时停止写正式文档并向用户说明：验证不足、结论冲突、需要拆分或需要结构性改写。

## 2. 触发时机与两种工作模式

Skill 默认在用户开始技术调研、方案设计、安装部署、测试验证、问题排查、交接或复盘相关工作时参与；不需要等到讨论结束后才手动触发。用户也可以显式调用 Skill，但不应把显式调用作为全程参与的前提。

Skill 在同一次工作中区分两种模式：

| 模式 | 触发时机 | Agent 要做什么 | 不做什么 |
|---|---|---|---|
| 过程整理模式 | 正在讨论、搜集资料、安装、测试、排错或做方案取舍，且用户尚未要求正式文档 | 在当前对话中持续归类：已验证事实、资料事实、已确认决定、未解决问题、应丢弃的过程噪音；提醒需要补充的验证或关键决策 | 不擅自生成正式文档；不把每轮对话自动写成文件；不把临时猜测当作结论 |
| 正式交付模式 | 用户明确要求总结、编写某类文档、输出手册/报告/清单，或确认当前工作已经完成并要求沉淀 | 从过程整理中只提取与目标读者有关、已验证或已确认的内容；执行类型路由、模板、自检和交付 | 不把调试过程、无效尝试、角色无关说明混入正式文档 |

过程整理模式中的“持续归类”默认保留在当前对话上下文中。只有用户明确要求保留过程记录，或工作量大到需要跨会话追踪时，Agent 才提出创建独立工作记录的建议，并等待用户确认后写入文件。

从过程整理模式转入正式交付模式时，Agent 必须复用已获得的信息；除非读者、文档目标或验证状态仍不明确，否则不得重复询问已经确认的问题。

## 3. 跨工具兼容原则

正式 Skill 采用通用 Agent Skills 核心：`SKILL.md`、`references/` 和可选的 `scripts/`、`assets/`。核心文件不得依赖 Codex 专用 YAML、Codex 工具名或某一厂商的命令语法。

- Codex、Claude Code 与 Cursor 分别通过其配置的 Skill 目录发现同一份核心包。
- 工具差异仅限安装路径、显式调用界面和自动发现机制；不改变文档路由、事实规则或模板内容。
- 不将 `agents/openai.yaml` 等厂商 UI 元数据放入通用核心包。需要平台专属显示设置时，后续以独立分发层提供，不能污染核心。
- 以 `.agents/skills/<skill-name>/` 作为优先的项目级通用安装目标；同时提供各工具原生目录的安装说明，并在发布前分别验证发现与引用加载。

## 4. Skill 的最终目录

```text
technical-documentation/
├─ SKILL.md
└─ references/
   ├─ common-rules.md
   ├─ scenario-guide.md
   ├─ document-splitting.md
   ├─ quality-gates.md
   ├─ research-basis.md
   └─ document-types/
      ├─ technical-research.md
      ├─ architecture-design.md
      ├─ feature-overview.md
      ├─ user-guide.md
      ├─ operations-runbook.md
      ├─ deployment-plan.md
      ├─ issue-analysis.md
      ├─ test-report.md
      ├─ handover-checklist.md
      ├─ service-onboarding.md
      └─ project-retrospective.md
```

第一版不创建 `examples/`、脚本或排版资产。它们目前不能解决已确认的核心问题，且容易把特定案例误写成通用规则。

## 5. 文件职责

| 文件 | 读取时机 | 只负责什么 | 不负责什么 |
|---|---|---|---|
| `SKILL.md` | Skill 启动时 | 区分过程整理与正式交付、识别任务、选择流程、读取规则、暂停条件、交付流程 | 不写 11 类模板的细节，不重复全部约束 |
| `common-rules.md` | 每次生成或结构性改写正式文档时 | 受众、逻辑、事实、语言、内容隔离、敏感信息、引用的共同规则 | 类型专属章节顺序 |
| `scenario-guide.md` | 用户未指定类型、类型混淆或需要多个文档时 | 场景判断、最少澄清问题、首选类型、常见误选类型 | 具体正文模板 |
| `document-splitting.md` | 内容多、角色多、已有文档需拆分时 | 必须/建议/不应拆分的条件、入口页、阅读路径、确认要求 | 正文写作规则 |
| `quality-gates.md` | 正式交付前 | 共同自检、类型自检的执行方式、发现问题后的处理分支 | 将检查清单写入交付正文 |
| `research-basis.md` | 用户询问模板依据、维护 Skill 或新增类型时 | 标准和成熟实践的来源、采用范围与不照搬原因 | 普通文档生成时的必读内容 |
| `document-types/*.md` | 已确认类型后 | 该类型的边界、主线、必须/可选/禁止内容、拆分条件和专属自检 | 其他类型的模板 |

## 6. Agent 的读取与执行流程

```text
收到调研、设计、实施、测试、排错、交接或复盘相关请求
  │
  ├─ 用户已要求正式文档或总结？
  │    ├─ 否：进入过程整理模式，在当前对话中持续归类事实、决定、待验证项和应丢弃的过程噪音
  │    └─ 是：进入正式交付模式
  │
  ├─ 用户明确指定文档类型？
  │    ├─ 是：读取 common-rules + 对应类型文件
  │    └─ 否：读取 scenario-guide
  │             │
  │             ├─ 能从请求确定一种类型：说明判断并继续
  │             └─ 不能确定：只问读者、目标、验证状态中的必要问题，等待确认
  │
  ├─ 验证未完成、证据冲突或结论不确定？
  │    └─ 是：暂停正式结论，说明缺失信息，等待用户指示
  │
  ├─ 内容需要拆分，或属于已有文档的结构性重写？
  │    └─ 是：读取 document-splitting，先提交文档树/重组方案，等待确认
  │
  ├─ 编写正式文档
  │    └─ 只使用共同规范与已确认类型的结构；不加载无关类型文件
  │
  └─ 交付前读取 quality-gates，内部自检后交付
```

## 7. 场景路由设计

`scenario-guide.md` 不直接问“你要哪种文档”，而是使用普通语言识别目标。

| 用户真正的目标 | 首选类型 | 需要确认的关键点 |
|---|---|---|
| 判断某技术、产品或方案能否满足业务要求 | 技术调研报告 | 是否已完成与需求对应的验证 |
| 记录已经确定的系统方案和关键取舍 | 架构设计/技术方案 | 读者是决策者、研发还是运维；是否需要拆分视图 |
| 解释一项能力是什么、适合谁、有什么限制 | 功能说明 | 是否需要进一步提供用户操作步骤 |
| 指导租户、普通用户或业务角色完成任务 | 使用说明 | 读者角色和单一任务目标 |
| 指导管理员、运维或实施人员管理系统 | 管理与运维手册 | 操作风险、权限、回退和升级边界 |
| 执行一次上线、切换、安装或部署 | 安装部署实施方案 | 环境、责任人、验证和回退是否明确 |
| 解释某次异常的现象、根因和修复 | 问题排查分析报告 | 是否是当前问题解决，还是需要形成复盘 |
| 判断已确定对象是否通过验收 | 测试报告 | 通过标准是否已定义 |
| 移交系统、资料、资产或长期责任 | 交接清单 | 接收方、接收确认和未决事项 |
| 跨角色申请或接入服务，并交付可用结果 | 接入申请与交付清单 | 各方输入、处理、交付物和验收 |
| 从项目或事件中形成改进闭环 | 项目总结/复盘报告 | 是否有行动项、负责人和跟踪机制 |

### 7.1 最少澄清问题

仅在请求无法可靠路由时，按顺序询问：

1. 这份文档主要给谁看？
2. 读者看完需要完成什么：理解、决定、操作、验收、交接，还是复盘改进？
3. 相关结论或操作是否已经完成验证？

若其中一项已有明确答案，不重复询问。

## 8. 共同规则的归并方式

130 条临时约束不应以 130 条平铺命令写入正式 Skill。正式 `common-rules.md` 按以下质量维度归并，每一维只保留会改变 Agent 决策的规则：

| 质量维度 | 关键约束 |
|---|---|
| 读者与目标 | 一个主要读者、一个主要目标；角色不同默认拆分；接入申请与交付清单是多参与角色的协作例外 |
| 事实与结论 | 区分已验证事实、资料事实、业务决定；未验证或冲突时暂停，不以模糊文字交付 |
| 阅读主线 | 按读者理解/执行顺序；后文不依赖未介绍概念；一个章节只解决一个问题 |
| 内容隔离 | 概念、任务、参考、排错、复盘不混写；正式文档不保留无效路径和过程碎念 |
| 表达方式 | 中文为主；术语一致；动作明确；删除空话、括号闲话、无信息标题和重复内容 |
| 证据与引用 | 关键结论可追溯；引用明确且有效；不堆放原始日志 |
| 安全与维护 | 不暴露敏感信息；适用时记录版本、负责人、验证时间；变更后复核 |
| 图表与代码 | 只在帮助理解时使用；图表紧邻引用；命令说明目的与成功判断 |

类型专属文件只补充本类型的章节顺序、可选条件、禁止内容、拆分方式和检查项，不能复制上述共同规则。

## 9. 类型文件的统一内部结构

所有 11 个类型文件采用相同的说明顺序，便于维护和定位：

1. 适用场景。
2. 不适用场景与应转向的类型。
3. 目标读者与阅读完成结果。
4. 阅读逻辑主线。
5. 必须章节：每章目的与允许内容。
6. 可选章节：触发条件。
7. 禁止内容。
8. 图、表、命令、截图和附录规则。
9. 拆分条件。
10. 类型专属自检。

这不是要求每份最终文档都展示上述十个章节；这是用于约束 Agent 的类型规范结构。

## 10. 需要保留的例外与停止条件

### 10.1 多角色例外

除“接入申请与交付清单”外，正式文档默认服务单一主要读者。接入申请与交付清单可面向多个参与角色，但必须以“角色/责任 → 输入或交付物 → 完成标准”表达，不能写成混杂的角色说明。

### 10.2 尚未验证

调研、测试、问题分析或实施结论缺少验证时，Skill 不允许生成正式结论。Agent 必须说明：已知事实、缺少的证据、可行的验证方法，以及需要用户决定的事项。

### 10.3 结构性改写与拆分

已有文档的重组、大段删除、角色拆分或多文件拆分，必须先提交拟调整结构和删除范围。用户确认前可以做事实核对和提出建议，不直接改写正文。

## 11. 阶段 B 的交付与验收

阶段 B 的交付物是本文件及同步更新的调研/约束/TODO 文件。阶段 B 视为完成必须满足：

1. 11 类文档均有唯一的类型文件归属。
2. 入口、共同规则、路由、拆分、自检、行业依据的职责不重叠。
3. 每一种请求都能说明要读取哪些文件。
4. 未指定类型、验证受阻、多文档拆分、已有文档重构均有明确处理路径。
5. 过程整理模式与正式交付模式的转换条件明确，且用户确认无需手动等待讨论结束才调用 Skill。
6. 核心包不含厂商专属元数据，且跨工具分发与验证方案明确。
7. 用户确认该目录、文件职责和读取流程。

阶段 B 完成后，才进入阶段 C，使用上述目录创建正式 Skill 文件。
