# 文档规范 Skill 后续任务 TODO

## 1. 任务目标

本任务要建立一套可复用的文档规范 Skill，用于约束 Agent 在调研、方案设计、测试、排错和项目收尾后，将零散对话、验证记录和资料整理为结构清晰、面向明确读者、可直接使用的正式文档。

该 Skill 要解决以下问题：

1. 防止 Agent 把对话过程、测试绕路、无效尝试和角色无关说明写入正式文档。
2. 防止文档按调试或测试发生顺序拼接，导致读者需要跳读、猜测前提或反复查找信息。
3. 为不同文档类型提供稳定、可裁剪的结构，避免每次生成的架构、调研、测试、操作等文档格式不同。
4. 强制区分已验证事实、资料事实和业务决定；不允许用模糊判断替代可验证结论。
5. 在文档内容过多时，指导 Agent 按读者、目标和职责拆分文档，并提供清晰入口和阅读路径。

## 2. 最终 Skill 应该是什么样子

最终产物不是一个包含所有模板的大文件，而是一套分层 Skill：

```text
technical-documentation/
├─ SKILL.md                         # 入口：触发条件、工作流程、场景路由
├─ references/
│  ├─ common-rules.md                # 全部文档共享的约束与交付前自检
│  ├─ scenario-guide.md              # 用户未指定类型时的场景判断与提问方式
│  ├─ document-splitting.md          # 文档拆分、入口页和导航规则
│  ├─ research-sources.md            # 采用的行业框架与适用边界
│  └─ document-types/
│     ├─ technical-research.md       # 技术调研报告
│     ├─ architecture-design.md      # 架构设计/技术方案
│     ├─ feature-overview.md         # 功能说明
│     ├─ user-guide.md               # 使用说明
│     ├─ operations-runbook.md       # 管理与运维手册
│     ├─ deployment-plan.md          # 安装部署实施方案
│     ├─ issue-analysis.md           # 问题排查分析报告
│     ├─ test-report.md              # 测试报告
│     ├─ handover-checklist.md       # 交接清单
│     ├─ service-onboarding.md        # 接入申请与交付清单
│     └─ project-retrospective.md    # 项目总结/复盘报告
└─ examples/                         # 可选：只保留经审阅的最小示例
```

### 2.1 入口文件必须做到的事

1. 判断用户是否明确指定了文档类型。
2. 未指定时，根据读者、目标、完成状态推荐一种类型，并等待用户确认。
3. 明确何时需要暂停：验证未完成、证据冲突、结论无法确认、结构性改写或大规模拆分。
4. 指向共同规范与相应的类型规范。
5. 要求 Agent 交付前执行内部自检，但不得将自检过程写进正式文档。

### 2.2 共同规范必须做到的事

1. 固化已确认的共同约束：受众隔离、逻辑递进、事实与证据、语言风格、图表使用、敏感信息、引用、拆分与验收。
2. 规定“正式文档只写最终有效路径”的处理方式。
3. 规定“结论不确定时先与用户讨论”的处理方式。
4. 规定哪些内容必须删除：过程流水账、角色无关说明、可删除括号注释、空泛提示、重复内容。
5. 规定最小交付质量门槛。

### 2.3 每个类型规范必须做到的事

1. 说明适用场景与不适用场景。
2. 说明目标读者与读者完成阅读后应获得的结果。
3. 给出单向、层层递进的阅读主线。
4. 给出必须章节、可选章节和禁止内容。
5. 说明适合使用的图、表、命令、截图和附录。
6. 说明何时拆分为入口文档和子文档。
7. 提供类型专属的交付前检查项。

## 3. 当前已完成的输入

| 输入 | 用途 | 状态 |
|---|---|---|
| `初版.md` | 原始 Skill 想法 | 已保留，作为历史参考 |
| `临时文档规范约束清单.md` | 127 条已确认共同约束 | 已完成 |
| `文档类型模板调研与裁剪建议.md` | 11 类文档的调研、模板骨架和裁剪建议 | 已完成，待审阅 |

## 4. 后续 TODO

### 阶段 A：确认模板边界

#### A1. 审阅 11 类文档的必须章节

**要做什么**

1. 逐类审阅 `文档类型模板调研与裁剪建议.md` 中的必须章节。
2. 确认哪些章节是所有该类型文档都必须包含的，哪些应改为按场景出现。
3. 删除只适合大型项目、强监管行业或特定云迁移项目的内容。
4. 补充用户日常高频、但调研未覆盖的内容。

**完成标准**

- 每种文档都拥有经用户确认的“必须、可选、禁止”边界。
- 不存在仅为了套行业模板而保留的章节。

#### A2. 确认类型之间的路由边界

**要做什么**

1. 核对技术调研与测试报告的分界。
2. 核对功能说明、使用说明与管理与运维手册的分界。
3. 核对问题排查分析与项目复盘的分界。
4. 核对工作交接清单与接入申请与交付清单的分界。
5. 确定用户未指定类型时，入口需要问的最少问题。
6. 明确一项工作需要多个关联文档时，Agent 只能建议，不得自动批量生成。

**完成标准**

- 任意一个典型请求都能被路由到一种首选文档类型。
- 交叉类型场景有清晰的拆分规则。

#### A3. 确认文档拆分规则

**要做什么**

1. 审核按读者、目标、内容职责、维护频率拆分的规则。
2. 确认入口文档包含的最少信息：范围、阅读路径、子文档用途、全局前置条件。
3. 确认何时必须先给用户展示文档树并等待确认。
4. 为大型架构、实施、运维和交接文档定义典型拆分示例。

**完成标准**

- Skill 能避免生成“一个大而全的长文档”。
- 拆分后仍保留完整阅读路径，不产生重复维护的内容。

### 阶段 B：设计 Skill 信息架构

#### B1. 确定最终目录、文件命名与引用关系

**要做什么**

1. 依据确认后的模板，确定最终目录结构。
2. 将 127 条约束归并到 `common-rules.md`、`scenario-guide.md` 和 `document-splitting.md`，避免多处重复。
3. 确定每个类型规范只包含自身特有内容，不复制共同约束。
4. 定义入口文件应读取哪些参考文件、何时读取。
5. 确定是否保留 `examples/`；如保留，仅收录最小、无敏感信息的示例。

**完成标准**

- 每一条约束都有唯一主要归属。
- 入口、共同规范、路由和类型文件的职责不重叠。
- Agent 可以按任务只读取必要文件，不必一次加载全部内容。

#### B2. 设计场景路由 Guide

**要做什么**

1. 把 11 类文档转换为普通用户能理解的场景问题。
2. 为未指定类型的请求设计不超过 3 个必要澄清问题。
3. 为每种场景给出首选类型、常见误选类型和推荐原因。
4. 定义已完成验证、验证进行中、验证受阻三种状态下的处理分支。
5. 定义多文档需求时的推荐方式和用户确认点。

**完成标准**

- 用户无需理解“技术调研报告”“架构设计”等术语也能选择正确类型。
- Agent 不会在验证未完成时直接输出正式结论。

#### B3. 设计交付前质量门槛

**要做什么**

1. 将 127 条约束整理为一份 Agent 内部使用的短检查表。
2. 为不同类型补充专属检查项。
3. 设计“发现问题后的处理”：删除、重组、补充验证、向用户提问或停止交付。
4. 确定质量检查不出现在正式文档正文中。

**完成标准**

- 检查表覆盖逻辑、读者、证据、可执行性、敏感信息和导航。
- 检查项简短到 Agent 能实际执行，不沦为冗长口号。

### 阶段 C：编写正式 Skill 文件

#### C1. 编写入口 `SKILL.md`

**要做什么**

1. 写明 Skill 的触发范围。
2. 写明“识别请求 → 选择/确认类型 → 收集材料 → 按类型规范编写 → 自检 → 交付”的工作流。
3. 写明引用其他文件的读取规则。
4. 写明用户明确指定类型、未指定类型、未完成验证、已有文档改写、大型文档拆分等分支。

**完成标准**

- Agent 在常见请求中知道先做什么、何时提问、何时不能下结论。
- 入口文件保持精炼，不塞入所有类型模板。

#### C2. 编写共同规范与路由文件

**要做什么**

1. 将确认后的共同约束写入 `common-rules.md`。
2. 将场景选择和提问方式写入 `scenario-guide.md`。
3. 将拆分、入口页和交叉链接规则写入 `document-splitting.md`。
4. 将行业框架、采用范围和不照搬的内容写入 `research-sources.md`。

**完成标准**

- 共同规则不与类型文件冲突。
- 场景路由能覆盖 11 类文档和验证受阻场景。

#### C3. 编写 10 个类型规范

**要做什么**

对每一个类型文件（共 11 个），统一编写以下内容：

1. 适用场景。
2. 不适用场景及应转向的文档类型。
3. 读者与阅读目标。
4. 阅读逻辑主线。
5. 必须章节及每章只允许写什么。
6. 可选章节及触发条件。
7. 禁止内容。
8. 图表、命令、截图和附录使用规则。
9. 拆分条件。
10. 专属自检。

**完成标准**

- 10 个文件的格式一致，但模板内容不被强行统一。
- Agent 能明确区分相近类型，不会混写。

### 阶段 D：场景验证与修订

#### D1. 路由测试

**要做什么**

使用至少以下场景测试入口：

1. “帮我调研某技术能否满足租户隔离需求。”
2. “给租户写一份创建项目的文档。”
3. “给管理员写一份配置配额的手册。”
4. “某服务部署后访问异常，整理问题分析。”
5. “功能已经确定，给出测试报告。”
6. “项目结束，要把系统交给运维。”
7. “这份架构文档太长，帮我拆分。”
8. “环境无法验证，帮我出调研结论。”

**完成标准**

- 每个场景都能路由到正确类型或正确暂停。
- 第 8 个场景必须触发用户确认，而不是生成模糊结论。

#### D2. 输出质量测试

**要做什么**

1. 为至少 5 类文档生成最小样例。
2. 检查是否按读者目标组织，而非按过程组织。
3. 检查是否存在角色污染、括号碎念、空标题、重复内容、未验证结论。
4. 检查操作文档是否可以从头顺序执行。
5. 检查拆分后的入口和子文档是否可导航。

**完成标准**

- 所有样例通过共同检查表和类型专属检查。
- 发现的问题回写到对应规范，而不是只修复样例。

#### D3. 用户验收与范围冻结

**要做什么**

1. 提交最终目录和关键文件供用户审阅。
2. 根据用户反馈处理必要修改。
3. 确认第一版不包含 Word/PDF 排版、行业合规模板、自动生成文档或外部知识库同步等扩展功能。
4. 将新增需求记录为后续版本候选，而不是在第一版继续扩张。

**完成标准**

- 用户确认 Skill 的文档类型、路由逻辑和质量标准。
- 第一版范围稳定且可以开始实际使用。

### 阶段 E：安装与实际试用

#### E1. 选择安装位置

**要做什么**

1. 确认是仅在当前项目中使用，还是安装为个人全局 Skill。
2. 若全局安装，保留当前工作目录中的可维护源文件。
3. 确认名称、触发方式和版本记录。

**完成标准**

- Skill 在目标环境可被发现和调用。
- 工作目录保留可审阅、可更新的源版本。

#### E2. 用真实任务试用

**要做什么**

1. 选择一项真实技术调研、管理与运维手册或问题分析任务。
2. 记录路由是否准确、提问是否必要、产出是否可直接使用。
3. 收集反复出现的问题，判断应更新共同规范、路由 Guide 还是某一类型规范。
4. 以小范围修订迭代，不改变已确认的核心原则。

**完成标准**

- 至少一个真实任务证明 Skill 可以减少过程污染、逻辑跳跃和模板不一致。
- 试用发现的问题形成明确的后续修订项。

## 5. 执行顺序与当前停点

建议严格按以下顺序执行：

1. 阶段 A：用户审阅并确认模板边界。已完成。
2. 阶段 B：设计正式 Skill 的信息架构和路由。进行中。
3. 阶段 C：编写 Skill 文件。
4. 阶段 D：用场景验证并修改。
5. 阶段 E：安装与真实试用。

**当前停点：阶段 B。**

阶段 B 产出正式 Skill 的目录、入口路由、参考文件职责与读取关系。阶段 B 经用户确认后，才创建正式 Skill 文件，避免把尚未确认的结构固化。
